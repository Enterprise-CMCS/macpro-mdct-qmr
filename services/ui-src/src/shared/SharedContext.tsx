import { createContext } from "react";

//WIP: this is scaffolding code as there's to many files to update at once, will remove once refactor is nearing complete
const SharedContext = createContext({});

export default SharedContext;
