import { ComponentStyleConfig } from "@chakra-ui/react";

const baseStyles = {
  helperText: {
    fontSize: "md",
    color: "black.700",
    lineHeight: "base",
  },
};

const formTheme: ComponentStyleConfig = {
  baseStyle: baseStyles,
};

export default formTheme;
