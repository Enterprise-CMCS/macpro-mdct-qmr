import * as QMR from "components";

export const FAQ = () => {
  return (
    <QMR.StateLayout breadcrumbItems={[{ path: `/faq`, name: "FAQ" }]}>
      <h1>This will be the FAQ Page</h1>
    </QMR.StateLayout>
  );
};
