name: Fill In Deployment PR

on:
  pull_request:
    types:
      - opened
    branches:
      - val
      - production

permissions:
  contents: write
  pull-requests: write

jobs:
  fill-in-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # using more advanced version for this action to be able to have node natively run typescript
          node-version: 22.19.0
      - uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.os }}-modules-${{ hashFiles('**/yarn.lock', 'plugins/**') }}
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Fill in Deployment PR
        run: |
          node .github/fill-in-deployment-pr.ts
        env:
          GITHUB_REPO: ${{ github.repository }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SOURCE_BRANCH: ${{ github.event.pull_request.head.ref}}
          TARGET_BRANCH: ${{ github.event.pull_request.base.ref }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          APP_NAME_UPPER: ${{ vars.APP_NAME_UPPER }}
